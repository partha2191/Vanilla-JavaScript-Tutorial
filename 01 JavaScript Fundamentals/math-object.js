const num1 = 100;
const num2 = 60;

let val;
// Simple Math Calculation
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.ceil(2.3);
val = Math.floor(2.9);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8, 2);
val = Math.min(8,55,47,559,1,0,2,-56);
val = Math.max(8,55,47,559,1,0,2,-56);
val = Math.random(); 
val = Math.fround(1.25);

val = Math.round(Math.random() * 10);

console.log(val);
