let id = 100;

// Test if any variables are Undefined
if(typeof id !== 'undefined'){
  console.log(`The ID is ${id}`);
} else{
  console.log('No ID');
}

// All the If conditions, Operators and SWITCH statemets are same as we used them in PHP.